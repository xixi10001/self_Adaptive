本仓库为多子模块的扁平结构：顶层仅保留 .gitignore，所有业务代码位于子目录（如 fire_simulation_datasets）。.gitignore 通过 __pycache__、*.pyc、.venv/、venv/、env/、.env、.vscode/、.idea/ 以及 **/models/、*.pth、*.pt、*.ckpt、*.safetensors 等规则，对所有子模块共享同一套忽略策略，避免将本地环境与训练产出纳入版本控制。