本仓库采用扁平多子模块结构：顶层仅保留共享的 .gitignore，所有业务代码下沉至子目录（如 fire_simulation_datasets）。.gitignore 通过 __pycache__、*.pyc、.venv/、venv/、env/、.env、.vscode/、.idea/ 以及 **/models/、*.pth、*.pt、*.ckpt、*.safetensors 等规则，对所有子模块施加同一套忽略策略，避免将虚拟环境、IDE 配置与训练产出纳入版本控制；子模块之间无跨包依赖，各自独立维护数据生成或训练逻辑。